{
  "name": "plvr_download_files",
  "version": "1.0.0",
  "description": "This Python script is designed to automate the download of PLVR land data from the official website. It uses the `requests` library to make HTTP requests and saves the data as ZIP files in a specified directory.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amazon-cognito-identity-js": "^6.3.12",
    "crypto-js": "^4.2.0"
  }
}
